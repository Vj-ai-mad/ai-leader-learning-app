import * as cdk from 'aws-cdk-lib'
import { Construct } from 'constructs'
// TODO T26-T30: Full API Stack implementation in Phase 1

export class ApiStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props: cdk.StackProps & Record<string, unknown>) {
    super(scope, id, props)
    // Stub — fully implemented in Phase 1 Task T26-T30
  }
}
